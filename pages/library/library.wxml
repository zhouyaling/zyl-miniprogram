<import src="../template/libraryClassCard/libraryClassCard.wxml" />
<view class="wrap">
  <view class="topNav">
    <van-tabs active="{{currentTab}}" animated color="#ffffff" swipeable bind:click="handlerOnChangeTab"  ellipsis="{{false}}"  tab-class="tab-reset-1" tab-active-class="tab-active-reset1">
      <van-tab wx:for="{{videoType}}" title="{{item.DictValue}}" name="{{item.DictKey}}"></van-tab>
    </van-tabs>
  </view>
  <view class="library-head-bk"> </view>
  <!--分类 -->
  <view class="library-tab">
    <view class="library-tab-item">
      <image src="../images/icon-lianxi.png" bindtap="changeType" data-type="1"></image>
      <view class="label">章节练习</view>
    </view>
      <view class="library-tab-item">
        <image src="../images/icon-kaoshi.png" bindtap="changeType" data-type="2"></image>
      <view class="label">模拟考试</view>
    </view>
    <view class="library-tab-item">
        <image src="../images/icon-ceshi.png" bindtap="changeType" data-type="3"></image>
          <view class="label">章节测试</view>
        </view>
        <view class="library-tab-item">
          <image src="../images/icon-zhenti.png" bindtap="changeType" data-type="4"></image>
          <view class="label">模拟真题</view>
        </view>
        <view class="library-tab-item">
        <image src="../images/icon-cuoti.png" bindtap="changeType" data-type="5"></image>
          <view class="label">我的错题</view>
        </view>
    </view>
    <!-- 内容区域 -->
    <view class="library-list">
    <template is="classCard" data="{{list}}"></template>
      <!-- <view class="library-list-item" wx:for="{{list}}" wx:key="id">
        <view class="library-list-total">
            <view style="height:90rpx;" data-item="{{item}}" bindtap="arrowClick">
              <view class="icon-check" class="{{item.status?'icon-check checked':'icon-check'}}" >
                <van-icon wx:if="{{item.status}}" name="arrow-up" color="#ffffff" />
                <van-icon wx:if="{{!item.status}}" name="arrow-down" color="#ffffff" />
              </view>
            </view>
            <view class="item-content">
              <view class="item-title">{{item.totalInfo.name}}</view>
              <view class="item-subtitle">{{item.totalInfo.actived}}/{{item.totalInfo.total}}道</view>
              <view class="progress-box">
                <progress percent="{{item.totalInfo.actived}}" color="#19CCB0" active stroke-width="3" />
              </view>
            </view>
            <image class="icon-edit" src="../images/icon-edit.png" bindtap="goQuestion" data-item="{{item.children[0]}}"></image>
        </view>
        <view class="library-list-detail" wx:if="{{item.status}}" wx:for="{{item.children}}" wx:for-item="child" wx:key="id" >
          <view class="">
            <van-icon name="circle" color="#19CCB0" />
            </view>
            <view class="item-content">
              <view class="item-title">{{child.name}}</view>
              <view class="item-subtitle">共{{child.total}}道题</view>
            </view>
            <image class="icon-edit" src="../images/icon-edit.png" bindtap="goQuestion" data-item="{{child}}"></image>
        </view>
      </view> -->
    </view>

      <view class="no-info" wx:if="{{list.length<=0}}">
        <image src="../images/no-info2.png"></image>
        <view>暂无数据，先看看其他的吧！</view>
      </view>
</view>
